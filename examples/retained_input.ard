use vaxis/retained
use vaxis/retained/input

fn main() {
  let field = mut input::new(value: "", placeholder: "Type here, then press Ctrl+C to quit")

  retained::run(mut field).expect("run retained input")
}
